# T�n ?ng d?ng
spring.application.name=backend

# ====================== MongoDB Atlas Connection ======================

# D�ng bi?n m�i tr??ng MONGODB_URI (khuy?n ngh?), fallback cho local/dev
spring.mongodb.uri=${MONGODB_URI:mongodb+srv://book_app:Dueminh1962004@cluster0.wq1wglc.mongodb.net/?appName=Cluster0}

# T�n database (c� th? b? n?u ?� c� trong URI)
spring.mongodb.database=book_app

# Logging
logging.level.org.springframework.data.mongodb.core=INFO

# Mobile app connection
server.address=${SERVER_ADDRESS:0.0.0.0}
server.port=${SERVER_PORT:8080}

# ====================== Mail SMTP ======================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dominhdue04@gmail.com
spring.mail.password=wbhkleulsucjymme
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ====================== Firebase Admin ======================
firebase.config.path=classpath:serviceAccountKey.json